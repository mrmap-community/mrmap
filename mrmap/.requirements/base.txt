Django==4.1.7 
# adds runserver_plus
django-extensions==3.2.1
# adds history models
django-simple-history==3.3.0
# adds filtersets for use in views
django-filter==21.1
# adds object permissions
django-guardian==2.4.0
# adds mptt tree model
django-mptt==0.14.0
# adds redis cache backend for django
django-redis==5.2.0
# adds captcha 
django-simple-captcha==0.5.17
# adds password hashing algorithm argon2
argon2-cffi==21.3.0
# adds cors header middleware for django to configure wildcard cors
django-cors-headers==3.14.0
# postgres sql adapter
psycopg2-binary==2.9.5
# adds class based xml mappers
eulxml==1.1.3
# library to process xml
lxml==4.9.2
# ISO 8601 date/time parser
isodate==0.6.1
# Python Imaging Library to create security images for example
Pillow==9.4.0
# http client lib
requests==2.28.2
# adds possiblity to export csv, excle, ods...
tablib==3.3.0
# YAML 1.1 parser
PyYAML==6.0
# wsgi application server
gunicorn==20.1.0
# asgi application server
uvicorn==0.21.1
# adds rest based views
djangorestframework==3.14.0
# adds geojson filters
djangorestframework-gis==1.0
# adds jsonapi implementation on top of django rest framework
djangorestframework-jsonapi==6.0.0
djangorestframework-jsonapi[django-filter]==6.0.0
# adds nested rest views registration
drf-extensions==0.7.1
# needed for openapi schema generation
uritemplate==4.1.1
# adds redis cache backed
redis==4.5.3
# adds background task processing 
celery==5.2.7
django-celery-beat==2.5.0
# adds database results for celery tasks
django-celery-results==2.5.0
# adds websockets
websockets==10.4
# adds websockets implementation for django
channels==4.0.0
channels[daphne]==4.0.0
# adds redis cache backend implementation for channels package
channels-redis==4.1.0
# Object mapper lib
odin==2.5


drf-spectacular==0.25.1
drf-spectacular-jsonapi==0.1.1

django-axis-order==0.1.0
django-ows-lib==0.4.0
import '@testing-library/jest-dom/extend-expect';
import { WmsSecuritySettings } from './WmsSecuritySettings';

describe('WmsSecuritySettings', () => {
  it('is defined', () => {
    expect(WmsSecuritySettings).toBeDefined();
  });
});

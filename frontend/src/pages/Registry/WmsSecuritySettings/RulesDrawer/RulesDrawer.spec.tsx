import '@testing-library/jest-dom/extend-expect';
import { RulesDrawer } from './RulesDrawer';

describe('RulesTable', () => {
  it('is defined', () => {
    expect(RulesDrawer).toBeDefined();
  });
});
